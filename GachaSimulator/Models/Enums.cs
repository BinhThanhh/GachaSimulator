namespace GachaSimulator.Models
{
    public enum ItemType {character = 0, weapon = 1}
    public enum ElementType {none = 0, pyro = 1, hydro = 2, anemo = 3, electro = 4, dendro = 5, cryo = 6, geo = 7}
    public enum BannerType {characterEvent = 0, weaponEvent = 1}
}
